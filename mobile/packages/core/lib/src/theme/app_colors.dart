import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const primary = Color(0xFF1A73E8);
  static const primaryDark = Color(0xFF1557B0);
  static const primaryLight = Color(0xFFD2E3FC);

  // Status
  static const success = Color(0xFF34A853);
  static const warning = Color(0xFFFBBC04);
  static const error = Color(0xFFEA4335);

  // Slot states
  static const slotAvailable = Color(0xFF34A853);
  static const slotReserved = Color(0xFFFBBC04);
  static const slotOccupied = Color(0xFFEA4335);
  static const slotBlocked = Color(0xFF9E9E9E);

  // Neutral
  static const background = Color(0xFFF8F9FA);
  static const surface = Colors.white;
  static const textPrimary = Color(0xFF202124);
  static const textSecondary = Color(0xFF5F6368);
  static const divider = Color(0xFFDADCE0);
}
